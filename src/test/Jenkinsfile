

pipeline{
    agent any

    stages{
        stage('Test stage') {
            steps{
                echo 'This is TEST stage'
                bat 'mvn clean test'
            }
        }
    }
    post{
        success{
            echo 'Test successed!'
            script {
                // Cucumber Report
                cucumber fileIncludePattern: '**/jupiterToysAutomation/target/cucumber.json', sortingMethod: 'ALPHABETICAL'
            }
        }
        failure {
            echo 'Test failed!'
        }
    }
}